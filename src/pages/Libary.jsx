import Layout from "../static/Layout";
import { Helmet } from "react-helmet";
export default function Library(props) {
  return (
    <Layout title="Library">
      <nav>Library page</nav>
    </Layout>
  );
}
